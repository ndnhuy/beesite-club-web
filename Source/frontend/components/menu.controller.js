'use strict';

angular
    .module('app.core')
    .controller('MenuController', function($window, $scope, $cookies, $log, AuthService) {
        var vm = this;
        
        // if ($cookies.get('username') == 'admin') {
        //     vm.adminMode = true;
        // }
    });